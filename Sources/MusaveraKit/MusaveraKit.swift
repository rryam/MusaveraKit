import Foundation
@_exported import MusicUnderstanding

/// MusaveraKit: one-line music understanding APIs for Apple's MusicUnderstanding framework.
public struct MusaveraKit {}

/// A compact facade for analyzing music assets.
///
/// Use `Musavera.analyze(asset:)` when you want all available analysis, or the
/// focused helpers like `Musavera.rhythm(for:)` when you only need one result.
public enum Musavera {}
